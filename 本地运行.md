# 本地编译运行

> 日常开发就看这一份。遇到报错再去看 `构建跑通指南.md`。

---

## 0. 前置一：装 Node.js（只做一次）

**如果 `npm run dev` 报 `bash: npm: command not found`，说明这一步没做。**

本机（2026-07-31 实测）**系统里没有安装 Node.js**。之前能构建成功，是因为 WorkBuddy
自带一份隔离的 Node（在 `~/.workbuddy/binaries/node/`），但那个 PATH 只在 WorkBuddy
启动的进程里生效，**你自己开的 Git Bash / PowerShell 继承不到**。

### 推荐做法：正规安装 Node.js LTS

1. 下载官方安装包：<https://nodejs.org/dist/v24.18.1/node-v24.18.1-x64.msi>
   （或去 <https://nodejs.org/en/download> 选 LTS 版本）
2. 双击安装，**保持默认勾选「Add to PATH」**
3. **关掉所有已开的终端窗口**，重新开一个 Git Bash（PATH 只在新开的终端里生效）
4. 验证：

```bash
node -v    # 应输出 v24.x.x
npm -v     # 应输出 11.x.x
```

装完之后本文所有命令都能直接跑。

### 装完如果 PowerShell 报「禁止运行脚本」

```
npm : 无法加载文件 C:\Program Files\nodejs\npm.ps1，因为在此系统上禁止运行脚本。
```

这是 PowerShell 的执行策略（默认 `Restricted`，禁跑所有 `.ps1`）。**在 PowerShell 里跑这一条**（只改当前用户，不需要管理员）：

```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned -Force
```

`RemoteSigned` = 允许本地脚本，远程下载的仍需签名，安全。设完 `npm -v` 就正常了。

> 用 Git Bash 不受此限制（Bash 调的是 `npm` 的 bash 版本，不走 `.ps1`）。
> 但既然你习惯用 PowerShell，就先把策略设好。

### 临时应急：借用 WorkBuddy 内置的 Node

不想装、只想马上跑起来，可以在**当前这个终端窗口**里临时加 PATH：

```bash
export PATH="/c/Users/yuyejin/.workbuddy/binaries/node/versions/22.22.2:$PATH"
node -v    # v22.22.2
npm -v     # 10.9.7
```

> **注意**：这个 `export` 只对当前终端窗口有效，关掉就失效，每次都要重敲。
> 而且那份 Node 属于 WorkBuddy 自身，版本可能随软件更新而变（路径里的 `22.22.2`
> 也会变），不适合长期依赖。**建议还是按上面正规装一个。**

### 想让临时方案永久生效（可选）

如果你就是不想装 Node，可以把 PATH 写进 Git Bash 的启动配置，这样每次开终端自动生效：

```bash
echo 'export PATH="/c/Users/yuyejin/.workbuddy/binaries/node/versions/22.22.2:$PATH"' >> ~/.bashrc
```

然后**重开 Git Bash**。缺点同上：WorkBuddy 更新后版本号变了就得改这一行。

---

## 0.5. 前置二：用 Git Bash，不要用 PowerShell

本文命令是 Bash 语法。**项目文件夹里右键 →「Git Bash Here」**，或在 PowerShell 里输 `bash` 回车。

在 PowerShell 里跑会报 `找不到与参数名称"sL"匹配的参数`、`无法将值"+%s"转换为类型"System.DateTime"` 之类的错——那是 shell 不对，不是项目坏了。

---

## 1. 日常开发（最常用）

```bash
cd /c/Users/yuyejin/Desktop/yuyeyyy.github.io
npm run dev
```

打开 <http://localhost:3000/>，改代码自动热更新，不用重启。

关于页在 <http://localhost:3000/about/>。

> **首次启动约 15 秒**，之后页面首访会现编译（关于页有 WebGL shader，稍慢），属正常。

### 停止

在终端按 `Ctrl + C`。

### 端口被占用

```bash
npx next dev -p 3001
```

---

## 2. 生产构建（部署前验证）

```bash
npm run build
```

成功的标志是看到**完整路由体积表**：

```
✓ Compiled successfully in 27.8s
✓ Generating static pages (24/24)
Route (app)                     Size  First Load JS
┌ ○ /                        ...
```

产物在 `out/`（本项目 `next.config.ts` 配的是 `output: "export"` 静态导出）。

### 预览构建产物

**不能用 `npm start`** —— 静态导出项目用 `next start` 会报 `prerender-manifest.json 不存在`。
要用静态服务器伺服 `out/`：

```bash
cd out && node -e "
const http=require('http'),fs=require('fs'),path=require('path');
const MIME={'.html':'text/html; charset=utf-8','.js':'text/javascript','.css':'text/css','.json':'application/json','.png':'image/png','.svg':'image/svg+xml','.txt':'text/plain; charset=utf-8','.xml':'application/xml','.woff2':'font/woff2'};
http.createServer((req,res)=>{
  let p=decodeURIComponent(req.url.split('?')[0]);
  if(p.endsWith('/')) p+='index.html';
  let f=path.join(process.cwd(),p);
  if(!fs.existsSync(f)&&fs.existsSync(f+'.html')) f=f+'.html';
  if(fs.existsSync(f)&&fs.statSync(f).isDirectory()) f=path.join(f,'index.html');
  try{const b=fs.readFileSync(f);res.writeHead(200,{'Content-Type':MIME[path.extname(f)]||'application/octet-stream'});res.end(b);}
  catch(e){res.writeHead(404);res.end('404');}
}).listen(4200,()=>console.log('http://localhost:4200'));
"
```

开 <http://localhost:4200/about/>。

> 每次改完代码想看生产效果，都要重新 `npm run build`（静态产物不会自动更新）。

---

## 3. 什么时候用 dev、什么时候用 build

| 场景                     | 用哪个                                       |
| ------------------------ | -------------------------------------------- |
| 改样式、改文案、调组件   | `npm run dev`（热更新，秒级反馈）            |
| 调 shader 参数           | `npm run dev`                                |
| 部署前最后检查           | `npm run build` + 静态服务器预览             |
| 想确认 SEO/SSR 输出      | `npm run build`，然后看 `out/xxx/index.html` |
| 检查有没有类型/lint 错误 | `npm run build`（它会跑 lint + 类型检查）    |

**日常 95% 的时间用 `npm run dev` 就够了。**

---

## 4. 其它常用命令

```bash
npm run lint      # 只跑 ESLint
npm run format    # prettier 格式化全项目
```

### 数据库（Cloudflare D1）

```bash
npm run db:local    # 初始化本地库
npm run db:init     # 初始化线上库
npm run db:export   # 从 D1 导出文章到 content/blog/.d1-export.json
npm run db:import   # 把本地文章导入 D1
npm run db:query "SELECT * FROM posts LIMIT 5"
```

### 部署

```bash
npm run cf:deploy         # 带 basePath
npm run cf:deploy:root    # 部署到根域名 yuyepage-3yi.pages.dev
```

> 部署脚本会自动串起 `db:export` → `build` → `index`（pagefind 建搜索索引）→ `wrangler deploy`。

---

## 5. 关于页 shader 的快速自查

改了 `components/about/about-backdrop-shader.ts` 或 `lib/about-data.ts` 后，
不想等完整 build，可以跑这几个脚本秒级校验：

```bash
node scripts/check-about-shader.cjs      # GLSL 结构：括号、uniform 作用域、函数闭合
node scripts/check-glsl-semantics.cjs    # GLSL 语义：swizzle 越界、float 赋整数字面量
node scripts/check-about-syntax.cjs      # TS/JSX 语法
node scripts/build-about-preview.cjs     # 生成独立预览页到 .workbuddy/preview/
```

预览页会内联真实 shader，**GPU 编译失败时会在页面左上角直接打出错误日志**——
这是验证 shader 能否真跑的最快手段。

---

## 6. 出错了怎么办

先按这个顺序判断：

```bash
# ① npm 本身找得到吗？
#   报 "bash: npm: command not found" → 没装 Node.js，回第 0 节
npm -v

# ② 是不是 shell 用错了？
#   报 "找不到与参数名称xx匹配的参数" → 你在 PowerShell 里，换 Git Bash

# ③ 环境是否完好？
ls node_modules/next/dist/compiled/commander/index.js   # 有输出 = next 包完好
ls -d .next 2>/dev/null || echo ".next 不存在（正常）"

# ④ 有被锁的缓存？（构建/dev 中断过会留下）
#   报 EPERM: open '.next\trace' → 用重命名而不是删除：
mv .next .next-locked-$(date +%s)
```

上面都排除后仍失败，去看 **`构建跑通指南.md`**——那份专门记录本机
`safe-delete` 工具引起的环境问题（`ETIMEDOUT`、`SAFE_DELETE_BULK_CONFIRM_REQUIRED`、
`Cannot find module .../commander/index.js` 等）及修复手法。

### 常见报错速查

| 报错                                              | 处理                                                         |
| ------------------------------------------------- | ------------------------------------------------------------ |
| `bash: npm: command not found`                    | 没装 Node.js，见第 0 节（这是最常见的）                      |
| `npm : 无法加载文件 ...npm.ps1，因为禁止运行脚本` | `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned -Force` |
| `EPERM: open '.next\trace'`                       | `mv .next .next-locked-$(date +%s)` 后重跑                   |
| `prerender-manifest.json 不存在`                  | 别用 `npm start`，用第 2 节的静态服务器                      |
| `端口 3000 已被占用`                              | `npx next dev -p 3001`                                       |
| `找不到与参数名称"sL"匹配的参数`                  | 你在 PowerShell 里，换 Git Bash                              |
| `SAFE_DELETE_BULK_CONFIRM_REQUIRED`               | 那一轮只跑构建这一条命令，重跑                               |
| `next 不是内部或外部命令`                         | 见 `构建跑通指南.md` 第二节                                  |

> **装完 Node 后记得重开终端**——PATH 变更不会作用于已经打开的窗口。
> 这是「明明装了却还说 command not found」的唯一原因。
