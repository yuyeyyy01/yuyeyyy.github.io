# Yuyeyyy 图形与渲染博客｜使用说明

> 一个基于 Next.js 15、Cloudflare Pages Functions 与 Cloudflare D1 的个人图形学博客。站点提供文章、WebGL 渲染实验、全文搜索、评论，以及在线写作与审核后台。

- 线上地址：<https://yuyepage-3yi.pages.dev>
- 默认部署模式：Cloudflare Pages 根路径部署
- 文章与评论数据源：Cloudflare D1（`yuyepage-db`）

---

## 1. 功能一览

| 模块     | 入口            | 用途                                                      |
| -------- | --------------- | --------------------------------------------------------- |
| 首页     | `/`             | 最新文章、精选实验、术语与统计信息                        |
| 博客     | `/blog/`        | 浏览文章列表、分类与标签内容                              |
| 文章页   | `/blog/<slug>/` | 阅读 Markdown/MDX 文章并提交评论                          |
| 实验室   | `/lab/`         | 浏览 WebGL2 渲染实验（如体积光、SDF 流体、SSR、卡通渲染） |
| 归档     | `/archive/`     | 按时间查看文章                                            |
| 搜索     | `/search/`      | 通过 Pagefind 搜索静态内容                                |
| 管理后台 | `/admin/`       | 管理员登录、文章 CRUD、评论审核                           |

### 文章发布机制

- 后台保存文章后，数据直接写入 D1。
- `/blog/*` 由 Cloudflare Pages Function 从 D1 动态渲染，CDN 缓存最长约 1 分钟；**发布或修改文章不需要重新构建站点**。
- 首页、实验室、关于页、搜索页等为静态构建内容。修改这些页面或前端代码后，需要重新部署。
- 文章图片不提供后台上传：请使用外链，或将图片提交到 `public/images/` 后以 `/images/文件名.png` 引用。

---

## 2. 环境要求

| 工具            | 要求                                                |
| --------------- | --------------------------------------------------- |
| Node.js         | 建议 Node.js 20 或更高版本                          |
| npm             | 与 Node.js 一同安装                                 |
| Cloudflare 账户 | 本地纯前端浏览可不需要；D1、Functions、部署必须需要 |
| Wrangler        | 已作为项目开发依赖安装，无需全局安装                |

首次使用 Cloudflare 命令前，先在项目目录执行：

```bash
npx wrangler login
```

> 下文所有命令都默认在项目根目录 `yuyeyyy.github.io` 中执行。

---

## 3. 首次本地启动

### 3.1 安装依赖

```bash
npm ci
```

若锁文件与依赖需要同步，也可以使用：

```bash
npm install
```

### 3.2 配置本地环境变量

在项目根目录新建 `.dev.vars`（该文件不应提交到 Git）：

```ini
ADMIN_TOKEN=replace-with-a-long-random-secret
NEXT_PUBLIC_BASE_PATH=
NEXT_PUBLIC_SITE_URL=http://localhost:8788
```

变量说明：

| 变量                    | 是否必填 | 用途                                                                           |
| ----------------------- | -------- | ------------------------------------------------------------------------------ |
| `ADMIN_TOKEN`           | 是       | 管理员登录 Cookie 的 HMAC 签名密钥。请使用高强度随机字符串。                   |
| `NEXT_PUBLIC_BASE_PATH` | 否       | 根路径部署时留空；仅需部署到子路径时设置，例如 `/my-site`。                    |
| `NEXT_PUBLIC_SITE_URL`  | 否       | 用于 SEO、RSS 与 Open Graph 的站点地址；本地建议设为 `http://localhost:8788`。 |

### 3.3 初始化本地 D1 数据库

依次执行两份迁移：

```bash
npm run db:local
npx wrangler d1 execute yuyepage-db --local --file=migrations/0002_admins.sql
```

第一条创建文章、评论与评论限流表；第二条创建管理员表。

如果需要把 `content/blog/*.mdx` 中的既有文章导入本地 D1，再执行：

```bash
npm run db:import
```

### 3.4 创建本地管理员账号

Windows PowerShell：

```powershell
$env:ADMIN_USERNAME="admin"
$env:ADMIN_PASSWORD="请换成至少6位的强密码"
node scripts/hash-password.mjs
```

macOS / Linux：

```bash
ADMIN_USERNAME=admin ADMIN_PASSWORD='请换成至少6位的强密码' node scripts/hash-password.mjs
```

该脚本会使用 PBKDF2-SHA256 生成密码哈希，并写入**本地** D1。账号已存在时会更新密码。

### 3.5 构建并启动本地 Pages 预览

```bash
npm run build
npm run index
npm run cf:dev
```

浏览器访问：

- 站点首页：<http://localhost:8788>
- 管理后台：<http://localhost:8788/admin/>

> `cf:dev` 用于验证 Cloudflare Pages Functions 与本地 D1。仅执行 `npm run dev` 时可以查看 Next.js 页面，但不会完整模拟 Pages Functions / D1 接口；需要测试登录、文章后台、评论时请使用上述 `build → index → cf:dev` 流程。

---

## 4. 内容与后台使用

### 4.1 写文章

1. 打开 `/admin/`，使用已创建的管理员账号与密码登录。
2. 切换到“文章”页签，选择“新建”。
3. 填写以下字段：

| 字段               | 规则                                                                          |
| ------------------ | ----------------------------------------------------------------------------- |
| Slug               | 仅可使用小写字母、数字与连字符，例如 `urp-volumetric-light`；创建后不可修改。 |
| 日期               | 建议使用 `YYYY-MM-DD`。                                                       |
| 标题 / 分类 / 摘要 | 用于文章页、列表与搜索展示。                                                  |
| 标签               | 使用逗号分隔。                                                                |
| 正文               | 填写 Markdown / MDX 源码。                                                    |
| 已发布             | 勾选后读者可从博客页访问；不勾选为草稿。                                      |

4. 点击保存。
5. 打开 `/blog/<slug>/` 检查内容。缓存刷新最多约 1 分钟。

### 4.2 审核评论

1. 进入 `/admin/` 并登录。
2. 默认“评论”页签展示待审核评论。
3. 对每条评论执行“通过”或“删除”。
4. 通过后，该评论会显示在对应文章页。

评论接口按 IP 做每日限流，每个 IP 每天最多提交 10 条。

### 4.3 本地文件方式维护文章

`content/blog/` 下的 MDX 文件可用于静态构建与历史内容维护；但线上博客的运行时数据源是 D1。

- 将已有 MDX 首次迁移到 D1：`npm run db:import`
- 从 D1 导出已发布文章到本地 MDX：`npm run db:export`
- 常规线上写作：优先使用 `/admin/`，不要在导入后同时以文件和 D1 作为两个事实来源。

---

## 5. 常用命令

| 命令                          | 作用                                                       |
| ----------------------------- | ---------------------------------------------------------- |
| `npm run dev`                 | 启动 Next.js 开发服务器；适合只调试页面 UI。               |
| `npm run build`               | 生成静态产物到 `out/`。                                    |
| `npm run start`               | 启动 Next.js 生产服务器；不包含 Pages Functions。          |
| `npm run index`               | 为 `out/` 生成 Pagefind 搜索索引。                         |
| `npm run cf:dev`              | 本地预览 Cloudflare Pages 静态产物、Functions 与本地 D1。  |
| `npm run db:local`            | 在本地 D1 执行 `0001_init.sql`。                           |
| `npm run db:init`             | 在远程 D1 执行 `0001_init.sql`。                           |
| `npm run db:import`           | 把本地 `content/blog/*.mdx` 批量导入 D1。                  |
| `npm run db:export`           | 从 D1 导出已发布文章到本地 MDX。                           |
| `npm run db:query -- "<SQL>"` | 对 D1 执行临时 SQL 查询。                                  |
| `npm run cf:deploy`           | 导出文章、构建、生成搜索索引并部署到 Cloudflare Pages。    |
| `npm run cf:deploy:root`      | 按根路径与 `https://yuyepage-3yi.pages.dev` 配置进行完整部署。 |
| `npm run lint`                | 执行项目定义的 Next.js lint 命令。                         |
| `npm run format`              | 使用 Prettier 格式化项目文件。                             |

> 搜索依赖 `pagefind` 索引。直接运行开发服务器时搜索不可用是正常现象；先执行 `npm run build && npm run index` 即可。

---

## 6. 首次部署到 Cloudflare Pages

### 6.1 远程 D1 初始化

确认 `wrangler.toml` 中的 D1 数据库配置指向目标数据库后，执行：

```bash
npm run db:init
npx wrangler d1 execute yuyepage-db --remote --file=migrations/0002_admins.sql
```

如需迁移既有本地文章：

```bash
npm run db:import
```

### 6.2 创建远程管理员账号

Windows PowerShell：

```powershell
$env:D1_REMOTE="1"
$env:ADMIN_USERNAME="admin"
$env:ADMIN_PASSWORD="请换成至少6位的强密码"
node scripts/hash-password.mjs
```

### 6.3 配置 Pages 环境变量

在 Cloudflare Dashboard 中打开对应 Pages 项目，进入 **Settings → Environment variables**，至少配置：

```ini
ADMIN_TOKEN=replace-with-a-long-random-secret
NEXT_PUBLIC_BASE_PATH=
NEXT_PUBLIC_SITE_URL=https://yuyepage-3yi.pages.dev
```

同时确认 Pages 项目已绑定名为 `yuyepage_db` 的 D1 数据库。`ASSETS` 是 Pages 的静态资源绑定，无需手动配置。

### 6.4 执行部署

```bash
npm run cf:deploy:root
```

部署完成后，依次验证：

1. 首页、博客、实验室和搜索页是否可正常访问。
2. `/admin/` 是否能登录。
3. 新建一篇草稿并发布，约 1 分钟后检查 `/blog/<slug>/`。
4. 提交一条评论，并在后台执行通过操作。

---

## 7. 项目结构

```text
app/                 Next.js 页面与后台 UI
components/          可复用组件、导航、实验室与主页组件
content/             静态文章与实验内容
functions/           Cloudflare Pages Functions（文章 SSR、评论、后台 API）
lib/                 站点配置、文章/实验读取工具
migrations/          D1 数据库迁移 SQL
scripts/             数据导入导出、管理员密码哈希脚本
public/              静态资源
wrangler.toml        Cloudflare Pages 与 D1 配置
```

关键文件：

| 文件                           | 作用                                            |
| ------------------------------ | ----------------------------------------------- |
| `lib/site.ts`                  | 站点 URL 与 `basePath` 的统一配置，默认根路径。 |
| `functions/blog/[[path]].ts`   | 从 D1 读取文章并渲染 `/blog/*`。                |
| `functions/api/admin/login.ts` | 管理员账号密码登录与 HttpOnly Cookie 签发。     |
| `migrations/0001_init.sql`     | 文章、评论、限流表结构。                        |
| `migrations/0002_admins.sql`   | 管理员账号表结构。                              |
| `scripts/hash-password.mjs`    | 创建或重置本地 / 远程管理员密码。               |

---

## 8. 排错速查

| 现象                          | 先检查什么                                                                                                              |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `/admin/` 登录后仍失败        | `.dev.vars` / Pages 环境变量中的 `ADMIN_TOKEN` 是否存在；管理员表是否已执行 `0002_admins.sql`；是否已运行密码哈希脚本。 |
| 本地后台接口 404 或数据库错误 | 不要只用 `npm run dev`；先 `npm run build && npm run index && npm run cf:dev`，并确认本地 D1 已初始化。                 |
| 搜索提示“索引尚未生成”        | 执行 `npm run build && npm run index` 后再启动预览。                                                                    |
| 新发布文章未立刻显示          | 确认文章“已发布”已勾选；等待最长约 1 分钟 CDN 缓存刷新；检查 slug 是否正确。                                            |
| 线上后台登录失败              | 确认远程 D1 已执行两份迁移、远程管理员已创建、Pages 环境变量已重新部署后生效。                                          |
| 页面链接出现错误前缀          | 检查 `NEXT_PUBLIC_BASE_PATH`：根路径部署必须为空，子路径部署时必须以 `/` 开头。                                         |
| 图片无法显示                  | 后台不上传图片；检查外链 URL 是否可公开访问，或确认文件已放入 `public/images/` 并重新部署。                             |

---

## 9. 安全与维护建议

- 不要提交 `.dev.vars`、管理员密码、`ADMIN_TOKEN` 或任何 Cloudflare 凭据。
- `ADMIN_TOKEN` 更换后，现有管理员 Cookie 会失效，用户需要重新登录。
- 线上账号密码通过 PBKDF2-SHA256 哈希存储；不要手工在数据库中填写明文密码。
- 在生产环境修改首页、组件、实验或样式后，运行 `npm run cf:deploy:root`；仅写博客文章时使用后台保存即可。
- `GISCUS_SETUP.md` 属于历史 Giscus 评论接入说明；当前站点评论以 D1 + 管理后台审核为准。
